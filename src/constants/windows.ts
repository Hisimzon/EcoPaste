/**
 * Tauri 窗口 label 常量。
 */
export const WINDOW_LABEL = {
  /**
   * 剪贴板窗口（剪贴板历史列表）。
   */
  CLIPBOARD: "clipboard",
  /**
   * 首次启动引导窗口。
   */
  ONBOARDING: "onboarding",
  /**
   * 偏好设置窗口。
   */
  PREFERENCE: "preference",
  /**
   * 剪贴板系统级预览 overlay 窗口。
   */
  PREVIEW: "clipboard-preview",
  /**
   * 软件更新窗口。
   */
  UPDATE: "update",
} as const;
